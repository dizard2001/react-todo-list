import React from 'react'
import PropTypes from 'prop-types'

export default class Card extends React.Component {
  constructor(props) {
    super(props)
    // get info out of props
  }

  render() {
    return (
      <div>
          <h1></h1>
      </div>
    );
  }
}

// export default componentName;
